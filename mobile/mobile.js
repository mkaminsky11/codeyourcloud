/*
1. link to websockets
2. check if logged in
	YES -> send to parent, handle it there
		a) send session info
	NO -> send to parent, handle it there
3. a change in title/text
	YES -> send it
	NO -> do nothing
4. collaborator change?
	YES -> send it
	NO -> do nothing
5. if asked for text
	a) get it
	b) send it
6. if asked for title
	a) get it
	b) send it
*/
